# Apple notarization credentials. Copy to .env and fill in.
# Identity cert ("Developer ID Application: Mili Software Inc. (PGZ497J325)")
# must already be in the login keychain — verify with:
#   security find-identity -v -p codesigning
APPLE_ID=
APPLE_PASSWORD=
APPLE_TEAM_ID=PGZ497J325

# Vercel project to deploy the landing page to (created on first deploy).
VERCEL_PROJECT_NAME=mili-db-explorer
